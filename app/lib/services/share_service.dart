import 'package:flutter/material.dart';

class ShareService {
  ShareService._();

  static Future<void> compartirProyecto(dynamic proyecto) async {
    debugPrint("Compartir disponible en la siguiente versión.");
  }
}