import 'package:flutter/material.dart';

class PdfService {
  PdfService._();

  static Future<void> generarProyecto(dynamic proyecto) async {
    debugPrint("PDF disponible en la siguiente versión.");
  }
}